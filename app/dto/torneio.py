class TorneioDTO(object):
    def __init__(self, nome, formato, grupos, pk, sorteado):
        self.nome = nome
        self.formato = formato
        self.grupos = grupos
        self.pk = pk
        self.sorteado = sorteado
